# Binaries
/bin/
/tmp/
*.exe
*.test
*.out
/ztna-api

# PAM replay store written by the seed harness / live gateway (runtime data)
/pam-replays/

# Local env / secrets
.env
.env.*
!.env.example

# Build artifacts
/dist/
coverage.txt

# Frontend (ui/) build + deps
/ui/node_modules/
/ui/dist/
# Embedded SPA build artifact (produced by `npm run build`, embedded under -tags embed_ui)
/internal/webui/dist/

# Security audit tooling cache + generated SBOM
/.tooling/
/dist/sbom.cyclonedx.json

# Blog evidence: export ZIPs are non-deterministic binary blobs (embedded
# generation timestamps); the verifiable manifest.json is committed instead.
blog/artifacts/payloads/*.zip

# Blog screenshot harness deps (Playwright); the committed package-lock pins them.
/blog/harness/screenshots/node_modules/
