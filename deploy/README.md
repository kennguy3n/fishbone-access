# Deployment manifests

Kubernetes manifests and Helm charts for ShieldNet Access live here.
The production Helm chart (deploy/helm/shieldnet-access) and Terraform
modules are added in Session 1F; the directory is reserved now so the
layout is stable.
