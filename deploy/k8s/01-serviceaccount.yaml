apiVersion: v1
kind: ServiceAccount
metadata:
  name: shieldnet-access
  namespace: shieldnet-access
  labels:
    app.kubernetes.io/name: shieldnet-access
    app.kubernetes.io/part-of: shieldnet-access
# The control plane never calls the Kubernetes API, so its token is not mounted.
automountServiceAccountToken: false
